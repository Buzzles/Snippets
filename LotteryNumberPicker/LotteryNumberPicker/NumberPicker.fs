#light

module NumberPicker

type NumberPicker (maxRange, amountOfNumbers) =
    let numberRange = [1..maxRange]
    member s.GetNumbers =
        let outarray =
        |> let x = [1..amountOfNumbers]    
        
        outarray
    

